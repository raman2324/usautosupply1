[build]
  command = "npm run build"
  publish = "dist/client"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["dist/server/**"]

[dev]
  command = "npm run dev"
  targetPort = 8080
  framework = "#custom"
  publish = "dist/client"
